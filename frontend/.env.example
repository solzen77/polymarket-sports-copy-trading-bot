# Backend API base URL (no trailing slash). Must be port 4004 where the API server runs.
# Run the backend from repo root: npm run api
# Do not use 3000 or 4000 (those are for the Next.js dev server).
NEXT_PUBLIC_API_URL=http://localhost:4004
